[
  {
    "name": "install_status",
    "tag": "Install",
    "request": { "method": "GET", "path": "/install/status" },
    "expected": { "status": 200, "success": true, "code": "INSTALL_STATUS" }
  },
  {
    "name": "install_migrate_unauthorized",
    "tag": "Install",
    "skip_if_locked": true,
    "request": { "method": "POST", "path": "/install/migrate", "body": {} },
    "expected": { "status": 401, "success": false, "code": "INSTALL_UNAUTHORIZED" }
  },
  {
    "name": "install_seed_unauthorized",
    "tag": "Install",
    "skip_if_locked": true,
    "request": { "method": "POST", "path": "/install/seed", "body": {} },
    "expected": { "status": 401, "success": false, "code": "INSTALL_UNAUTHORIZED" }
  },
  {
    "name": "install_complete_unauthorized",
    "tag": "Install",
    "skip_if_locked": true,
    "request": { "method": "POST", "path": "/install/complete", "body": {} },
    "expected": { "status": 401, "success": false, "code": "INSTALL_UNAUTHORIZED" }
  },
  {
    "name": "install_migrate_authorized",
    "tag": "Install",
    "skip_if_locked": true,
    "request": {
      "method": "POST",
      "path": "/install/migrate",
      "headers": { "X-Install-Token": "{{install_token}}" },
      "body": {}
    },
    "expected": { "status": 200, "success": true, "code": "INSTALL_MIGRATED" }
  },
  {
    "name": "install_seed_authorized",
    "tag": "Install",
    "skip_if_locked": true,
    "request": {
      "method": "POST",
      "path": "/install/seed",
      "headers": { "X-Install-Token": "{{install_token}}" },
      "body": { "admin_password": "admin123" }
    },
    "expected": { "status": 200, "success": true, "code": "INSTALL_SEEDED" }
  }
]
